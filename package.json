{
  "name": "welding-app",
  "version": "0.1.0",
  "homepage": "https://alekseishchemelev.github.io/sawWeld",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "clean": "if exist build rmdir /s /q build & if exist node_modules\\.cache rmdir /s /q node_modules\\.cache",
    "build:clean": "npm run clean && react-scripts build",
    "predeploy": "npm run build:clean",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.8.2",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
